<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <Button
        android:id="@+id/testGpsButton"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:text="GPS Diagnostics" />

    <Button
        android:id="@+id/testSensorButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_toRightOf="@+id/testGpsButton"
        android:text="Orientation Diagnostics" />

    <Button
        android:id="@+id/trackpadDiagnosticsButton"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/testGpsButton"
        android:layout_alignParentLeft="true"
        android:text="Trackpad Diagnostics" />

    <Button
        android:id="@+id/testAuthenticationButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/testSensorButton"
        android:layout_toRightOf="@+id/testGpsButton"
        android:text="Test Authentication" />
    
    <Button
        android:id="@+id/testSyncButton"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/trackpadDiagnosticsButton"
        android:layout_alignParentLeft="true"
        android:text="Test Sync" />

    <TextView
        android:id="@+id/mainTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/testAuthenticationButton"
        android:layout_toRightOf="@+id/testSyncButton"
        android:text="MainTextView" />

</RelativeLayout>